# System Architecture

> Auto-generated by `scripts/graphify` on 2026-09-14 from the actual repository files. Do not edit by hand — run `npm run graphify` to regenerate.

## Diagram

```mermaid
flowchart TB
classDef frontend fill:#f0fdf4,stroke:#16a34a,color:#14532d
classDef backend fill:#eff6ff,stroke:#2563eb,color:#1e3a8a
classDef authc fill:#fff7ed,stroke:#ea580c,color:#7c2d12
classDef data fill:#fdf4ff,stroke:#a21caf,color:#701a75
classDef external fill:#f1f5f9,stroke:#64748b,color:#334155
classDef util fill:#f8fafc,stroke:#94a3b8,color:#334155

subgraph L1["1 • Client — Browser"]
  L1a["Next.js 14 · React 18 App Router"]:::frontend
  L1b["Route groups: (auth) login · (dashboard) · (pos) POS"]:::frontend
  L1c["Server components (SSR) — dashboard & list pages"]:::frontend
  L1d["Client components — POS, catalogs, tables, dialogs"]:::frontend
  L1e["shadcn/ui primitives (src/components/ui)"]:::frontend
  L1f["State: Zustand ui-store + React hooks"]:::frontend
  L1g["Data fetching: native fetch() + SWR-style hooks (no axios / no react-query yet)"]:::frontend
end

subgraph L2["2 • Edge"]
  L2a["src/middleware.ts — next-auth withAuth"]:::authc
  L2b["Redirects unauthenticated users to /login; /pos requires permission sales:create"]:::authc
  L2c["Matcher: every path except login, register, reset-password, api/auth, static assets"]:::authc
end

subgraph L3["3 • Server — Next.js App Router"]
  L3a["API routes /api/... (40 handlers)"]:::backend
  L3b["Server actions: none (all mutations go through API routes)"]:::util
  L3c["Server-side auth gates — getServerSession + requirePermission()"]:::authc
  L3d["Domain services under src/lib — products · inventory · batches (FEFO, expiry) · sales · settings · organization"]:::backend
  L3e["Zod validation schemas under src/lib/validations"]:::backend
  L3f["Dashboard & product/batch/inventory pages query Prisma directly (Server Components)"]:::backend
end

subgraph L4["4 • Authentication"]
  L4a["NextAuth v4 — /api/auth/[…nextauth]"]:::authc
  L4b["Credentials provider · Prisma adapter · JWT sessions (30 days)"]:::authc
  L4c["bcrypt password verify · lockout after 5 failures for 15 min"]:::authc
  L4d["JWT carries permissions[] · roles[] · branchId"]:::authc
  L4e["src/lib/auth — auth-config · auth-helpers (requireAuth, can, requirePermission)"]:::authc
end

subgraph L5["5 • Data Access"]
  L5a["Prisma Client singleton — src/lib/db/prisma.ts"]:::data
  L5b["Concurrency-safe patterns — updateMany CAS (updatedAt), Serializable isolation, retry"]:::data
end

subgraph L6["6 • Database — PostgreSQL"]
  L6a["Prisma schema — 50 models · 19 enums · 60 FK relations"]:::data
  L6b["Tables incl. users, roles, permissions, products, batches, inventory, sales, purchases, audit_logs"]:::data
  L6c["Seed scripts — roles, permissions, org, HSN, categories, sample products"]:::data
end

subgraph L7["7 • External / Configured"]
  L7a["SMTP email (nodemailer configured in env — not yet used in code)"]:::external
  L7b["Local file storage UPLOAD_DIR (configured — not yet used in code)"]:::external
  L7c["papaparse — used by product CSV import"]:::external
end

L1a --> L2a
L1b --> L1a
L1c --> L1a
L1d --> L1e
L1d --> L1f
L1d --> L1g
L1g -->|"fetch(/api/…)"| L3a
L2a --> L2b
L2a --> L3a
L2a --> L3b
L3a --> L3d
L3a --> L3e
L3c --> L4a
L3f --> L4a
L3d --> L5a
L4a --> L5a
L5a --> L6a
L6a --> L6b
L6a --> L6c
L4a --> L4b
L4b --> L4c
L4c --> L4d
L4e --> L4a
L7c --> L3d
```

## What this graph represents

How the PharmaCare layers fit together: Next.js App Router UI, NextAuth JWT sessions, domain services, API routes, the Prisma client and PostgreSQL. Drawn from the actual files in src/ and prisma/.

## Source files

- `src/app/** (route groups layout.tsx / page.tsx)`
- `src/components/**`
- `src/middleware.ts`
- `src/lib/**`
- `prisma/schema.prisma`
- `next.config.js`

_See [../README.md](../README.md) for how to view and regenerate._

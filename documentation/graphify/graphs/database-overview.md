# Database Overview — all 50 models by module

> Auto-generated by `scripts/graphify` on 2026-09-14 from the actual repository files. Do not edit by hand — run `npm run graphify` to regenerate.

## Diagram

```mermaid
flowchart LR
classDef mod fill:#f8fafc,stroke:#94a3b8,color:#1e293b


subgraph DB_auth["Authentication"]
    User["User"]:::mod
    Account["Account"]:::mod
    Session["Session"]:::mod
    VerificationToken["VerificationToken"]:::mod
    PasswordResetToken["PasswordResetToken"]:::mod
end

subgraph DB_roles["Roles & Permissions"]
    Role["Role"]:::mod
    Permission["Permission"]:::mod
    RolePermission["RolePermission"]:::mod
    UserRole["UserRole"]:::mod
end

subgraph DB_organization["Organization"]
    Organization["Organization"]:::mod
    Branch["Branch"]:::mod
    OrganizationSetting["OrganizationSetting"]:::mod
end

subgraph DB_products["Products"]
    Category["Category"]:::mod
    Product["Product"]:::mod
    ProductCategory["ProductCategory"]:::mod
    ProductBarcode["ProductBarcode"]:::mod
end

subgraph DB_gst["GST / Tax"]
    HsnCode["HsnCode"]:::mod
    TaxRate["TaxRate"]:::mod
    GstTransaction["GstTransaction"]:::mod
end

subgraph DB_inventory["Inventory"]
    Inventory["Inventory"]:::mod
    InventoryMovement["InventoryMovement"]:::mod
    StockAdjustment["StockAdjustment"]:::mod
end

subgraph DB_batches["Batches"]
    Batch["Batch"]:::mod
    BatchStatusLog["BatchStatusLog"]:::mod
    BatchDisposal["BatchDisposal"]:::mod
end

subgraph DB_sales["Sales"]
    Sale["Sale"]:::mod
    SaleItem["SaleItem"]:::mod
    SaleItemBatch["SaleItemBatch"]:::mod
    Payment["Payment"]:::mod
end

subgraph DB_pos["POS / Billing"]
    HeldBill["HeldBill"]:::mod
end

subgraph DB_prescriptions["Prescriptions"]
    Prescription["Prescription"]:::mod
    PrescriptionImage["PrescriptionImage"]:::mod
end

subgraph DB_purchases["Purchases"]
    Purchase["Purchase"]:::mod
    PurchaseItem["PurchaseItem"]:::mod
    PurchaseReturn["PurchaseReturn"]:::mod
    PurchaseReturnItem["PurchaseReturnItem"]:::mod
end

subgraph DB_customers["Customers"]
    Customer["Customer"]:::mod
    CustomerLedger["CustomerLedger"]:::mod
end

subgraph DB_suppliers["Suppliers"]
    Supplier["Supplier"]:::mod
    SupplierLedger["SupplierLedger"]:::mod
end

subgraph DB_returns["Returns"]
    SaleReturn["SaleReturn"]:::mod
    SaleReturnItem["SaleReturnItem"]:::mod
    CreditNote["CreditNote"]:::mod
end

subgraph DB_finance["Finance"]
    Ledger["Ledger"]:::mod
    LedgerEntry["LedgerEntry"]:::mod
end

subgraph DB_notifications["Notifications"]
    Notification["Notification"]:::mod
    NotificationPreference["NotificationPreference"]:::mod
end

subgraph DB_audit["Audit"]
    AuditLog["AuditLog"]:::mod
end

subgraph DB_settings["Settings"]
    SystemSetting["SystemSetting"]:::mod
end

subgraph DB_files["Files"]
    File["File"]:::mod
end
```

## What this graph represents

Every Prisma model grouped by its business module. This is a structural overview; the three following ERDs show fields and FK relations per domain.

## Source files

- `prisma/schema.prisma`

_See [../README.md](../README.md) for how to view and regenerate._
